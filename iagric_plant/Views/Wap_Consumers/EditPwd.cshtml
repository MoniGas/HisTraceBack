@{
    Layout = null;
}
@*<html>
<head>
    <meta charset="utf-8">
    <title>医疗器械（UDI）服务云平台</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0,user-scalable=no"
        id="viewport">
    <script src="/Scripts/jquery-1.7.2.min.js" type="text/javascript"></script>
    <script src="/Scripts/glvalidate.js" type="text/javascript"></script>
    <link href="/css/home.min.css" rel="stylesheet" type="text/css" />
    <link href="/css/style.min.css" rel="stylesheet" type="text/css" />
    <script type="text/javascript">
        $(function () {
            $("#BtnSubmit").click(function () {
                if (isNull($("#TxtOldPwd").val())) {
                    alert("请输入原密码");
                    return false;
                }
                if (isNull($("#TxtNewPwd").val())) {
                    alert("请输入新密码");
                    return false;
                }
                if ($.trim($("#TxtNewPwd").val()).length < 6 || $.trim($("#TxtNewPwd").val()).length > 20) {
                    alert("请输入6-20位的密码！");
                    return false;
                }
                if (isNull($("#TxtSurePwd").val())) {
                    alert("请输入确认密码");
                    return false;
                }
                if ($("#TxtNewPwd").val() != $("#TxtSurePwd").val()) {
                    alert("两次输入的密码不一致");
                    return false;
                }
                $.ajax({
                    type: 'POST',
                    url: "/Wap_Consumers/EditPwd",
                    data: $("#formPwd").serialize(),
                    success: function (data) {
                        if (data.info != null && data.info != "") {
                            alert(data.info);
                        }
                        if (data.res) {
                            if (data.url != null && data.url != "") {
                                window.location.href = data.url;
                            }
                        }
                    }
                });
            })
        });
    </script>
</head>
<body>
    <!-- 页头 -->
    <section>
	<header class="headerbox">
    <a href="/Wap_Consumers/Index" class="art-back">返回</a>
		<h1 title="修改密码">修改密码</h1>
	</header>
</section>
    <!--内容-->
    <div class="scroll-wrap" style="border-bottom: 1px solid #e7e7e7;">
        <div class="scroll-content">
            <div class="item-list o-t-item" style="background: #fff;">
                <div class="item-img" style="background: #f5f5f5;">
                    <p>
                        <img src="/images/wapnew/haed.jpg" /></p>
                </div>
                <div class="item-info">
                    <h3 class="title mat10">@ViewBag.linkPhone</h3>
                </div>
            </div>
            <div class="box-mg">
                <form name="formPwd" id="formPwd">
                <input type="hidden" name="TxtConsumersId" id="TxtConsumersId" value="@ViewBag.consumersId">
                <dl class="list list-in">
                    <dd>
                        <dl>
                            <dd class="dd-padding kv-line">
                                <h6>
                                    原密码：
                                </h6>
                                <input class="input-weak kv-v" type="text" id="TxtOldPwd" name="TxtOldPwd" placeholder="请输入原密码"
                                    maxlength="20" />
                            </dd>
                            <dd class="dd-padding kv-line">
                                <h6>
                                    新密码：
                                </h6>
                                <input class="input-weak kv-v" type="password" placeholder="请输入新密码" id="TxtNewPwd"
                                    name="TxtNewPwd" maxlength="20" />
                            </dd>
                            <dd class="dd-padding kv-line">
                                <h6>
                                    确认密码：
                                </h6>
                                <input class="input-weak kv-v" type="password" placeholder="请输入确认密码" id="TxtSurePwd"
                                    name="TxtSurePwd" maxlength="20" />
                            </dd>
                        </dl>
                    </dd>
                </dl>
                <section class="section-module mat10">
          <input type="button" class="orange-btn-l" id="BtnSubmit" name="BtnSubmit" value="保存">
	</section>
                </form>
            </div>
        </div>
    </div>
    <div class="juchou-footer">
        <footer>
            <p>医疗器械（UDI）服务云平台</p>
        </footer>
    </div>
</body>
</html>
*@
<html>
<head>
    <meta charset="UTF-8">
    <title>医疗器械（UDI）服务云平台</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0,user-scalable=no"
        id="viewport">
    <link rel="stylesheet" type="text/css" href="../public_css/base.css">
    <link rel="stylesheet" type="text/css" href="../public_css/public-style.css">
    <link rel="stylesheet" type="text/css" href="../public_css/login.css">
    <script src="/Scripts/jquery-1.7.2.min.js" type="text/javascript"></script>
    <script src="/Scripts/glvalidate.js" type="text/javascript"></script>
    <script type="text/javascript">
        document.documentElement.style.fontSize = document.documentElement.clientWidth / 7.5 + 'px';
		</script>
    <script type="text/javascript">
        $(function () {
            $("#BtnSubmit").click(function () {
                if (isNull($("#TxtOldPwd").val())) {
                    alert("请输入原密码");
                    return false;
                }
                if (isNull($("#TxtNewPwd").val())) {
                    alert("请输入新密码");
                    return false;
                }
                if ($.trim($("#TxtNewPwd").val()).length < 6 || $.trim($("#TxtNewPwd").val()).length > 20) {
                    alert("请输入6-20位的密码！");
                    return false;
                }
                if (isNull($("#TxtSurePwd").val())) {
                    alert("请输入确认密码");
                    return false;
                }
                if ($("#TxtNewPwd").val() != $("#TxtSurePwd").val()) {
                    alert("两次输入的密码不一致");
                    return false;
                }
                $.ajax({
                    type: 'POST',
                    url: "/Wap_Consumers/EditPwd",
                    data: $("#formPwd").serialize(),
                    success: function (data) {
                        if (data.info != null && data.info != "") {
                            alert(data.info);
                        }
                        if (data.res) {
                            if (data.url != null && data.url != "") {
                                window.location.href = data.url;
                            }
                        }
                    }
                });
            })
        });
    </script>
</head>
<body>
    <header class="OriginHead bar-green">
        <a href="/Wap_Consumers/SheZhi" class="bar-item pull-left"><i class="icon bar-arrow-left">
        </i></a>
        <h1 class="bar-title">
            修改密码</h1>
    </header>
    <div class="coupon-content">
        <div class="weui-cells weui-cells_form">
            <form name="formPwd" id="formPwd">
            <input type="hidden" name="TxtConsumersId" id="TxtConsumersId" value="@ViewBag.consumersId">
            <div class="weui-cell">
                <div class="weui-cell__hd">
                    <label class="weui-label">
                        原密码</label></div>
                <div class="weui-cell__bd">
                    <input class="weui-input" type="password" id="TxtOldPwd" name="TxtOldPwd" maxlength="20"
                        placeholder="请输入原密码">
                </div>
            </div>
            <div class="weui-cell">
                <div class="weui-cell__hd">
                    <label class="weui-label">
                        新密码</label></div>
                <div class="weui-cell__bd">
                    <input class="weui-input" type="password" id="TxtNewPwd" name="TxtNewPwd" maxlength="20"
                        placeholder="请输入新密码">
                </div>
            </div>
            <div class="weui-cell">
                <div class="weui-cell__hd">
                    <label class="weui-label">
                        确认密码</label></div>
                <div class="weui-cell__bd">
                    <input class="weui-input" type="password" placeholder="请输入确认密码" id="TxtSurePwd" name="TxtSurePwd"
                        maxlength="20">
                </div>
            </div>
            </form>
        </div>
        <div class="page__bd page__bd_spacing" style="margin-top: .4rem;">
            <a href="#" class="syui-btn syui-btn_primary" id="BtnSubmit" name="BtnSubmit">保存</a>
        </div>
    </div>
</body>
</html>
